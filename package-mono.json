{
  "name": "syndi-ai-root",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev":        "turbo dev",
    "dev:web":    "npm run dev --workspace=@syndi/web",
    "dev:legacy": "npm run dev --workspace=@syndi/legacy",
    "build":      "turbo build"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
