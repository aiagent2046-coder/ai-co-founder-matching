# Handoff — фича «Агенты» (Путь A)

> Документ для следующей сессии. Мы (Don + AI-сооснователь) договорились строить
> систему агентов-ассистентов проекта по **Пути A — фундамент сначала, инкрементальные MVP**.
> Прервались на этапе планирования по просьбе Don. Стартуем с MVP-1.

## Где остановились (факты на 25.06.2026, 23:51 MSK)

- **Домен `syndimatch.online` ПОЛНОСТЬЮ РАБОТАЕТ.** DNS починен (apex A → `216.150.1.1`,
  www CNAME → `b7754d14630192e4.vercel-dns-016.com`, оба на Vercel). HTTPS 200. SSL ок.
- **Серверный auth (Path A прошлой задачи) работает.** Don проверил вживую: вход
  зарегистрированного, регистрация нового, прохождение тестов, чат — всё ок.
- **Единственный сломанный экран:** `/app/agents` → **404**. Файла нет в `main`.
- `main` HEAD = `71dde92`. Рабочая директория чистая.

## Что хочет Don (видение — НЕ менять без него)

Не чат-боты с промптами и НЕ статическая витрина. Нужен **AI-экзоскелет проекта** —
команда специализированных агентов, как работает сам Don со своим AI-сооснователем:

| Агент | Роль |
|-------|------|
| 🧑‍💼 HR | подбор со-фаундеров / команды |
| 👨‍💻 Программист | работа с кодом |
| 📣 Маркетинг | стратегия продвижения |
| 📢 PR (Don сократил «ЗК») | внешние коммуникации, репутация |
| ⚖️ Юрист | риски, раскрытие информации, комплаенс — ОБЯЗАТЕЛЕН |

**Ключевые свойства («золотая середина»):**
- Свой промпт/специализация у каждого.
- Понимают **весь контекст проекта** (профиль, матчи, диалоги).
- **Работают в фоне** (не только по запросу).
- **Проактивны** — сами предлагают стратегию, сами предупреждают о рисках
  (пример Don: «предлагают маркетинговую стратегию» / «предупреждают о рисках в связи с раскрытием информации»).
- Цель: полная синергия человека и AI.

## План — Путь A (инкрементально, каждый шаг = отдельный PR)

### Шаг 0 — ИЗУЧИТЬ КОД ПЕРЕД ПЛАНОМ (обязательно, Karpathy: думай перед кодингом)
Прочитать и понять реальную архитектуру, НЕ выдумывать:
- `app/app/chat/page.tsx`, `app/app/chat/[matchId]/page.tsx` — как устроен чат UI.
- `app/api/messages/route.ts` — как идут сообщения через бэкенд.
- Как вызывается Claude / AITunnel (найти грепом по `anthropic` / `claude` / `aitunnel`).
- `lib/supabase-server.ts` — `getServerUser()` для контекста пользователя.
- `app/api/matches/list/route.ts`, RPC `match_founders` — доступ к данным проекта.

### Шаг 1 — MVP-1 (цель следующей сессии): живые агенты вместо 404
- `app/app/agents/page.tsx` — список 5 РЕАЛЬНЫХ ролей (см. таблицу выше).
- Клик по агенту → чат с этим агентом, у каждого свой системный промпт + доступ
  к контексту проекта (профиль, матчи). Переиспользовать существующий чат-бэкенд.
- Это уже работающие агенты, отвечающие по делу. НЕ витрина.

### Шаг 2 — MVP-2: фон
- Фоновые задачи: агент периодически анализирует проект → пишет инсайты/предупреждения
  в ленту уведомлений.

### Шаг 3 — MVP-3: проактивность
- Триггеры: юрист реагирует на раскрытие данных, маркетинг — на новые матчи, и т.д.

## ВАЖНО: старый файл agents — это НЕ то, что нужно

В ветке `origin/fix/consolidate-ratelimit` лежит `app/app/agents/page.tsx`, НО это
**статическая демо-витрина** (захардкоженные 5 «агентов матчинга» MatchAgent/PersonalityAgent/
ChatAgent/AvatarAgent/InsightAgent + фейковый анимированный live-log). Это НЕ роли Don и НЕ
работающая фича. Можно взять оттуда ТОЛЬКО визуальный стиль карточек (классы `card`,
`gradient-text`, `font-display`, анимации `twinkle`/`fade-in` — все есть в `app/globals.css`),
но логику и список агентов строить заново по видению выше.

## Рабочие договорённости с Don (Karpathy Guidelines)
- Думай перед кодингом: озвучивай допущения, называй интерпретации, не выбирай молча.
- Простота прежде всего: минимальный код, ничего спекулятивного.
- Хирургические изменения: трогай только нужное.
- Не выдумывай несуществующие методы/конфиги.
- Доказательная диагностика: проверяй фактами (curl/dig/logs/CI) ПЕРЕД фиксом.
- Предлагай 2 именованных пути (Путь A/B) на развилках.
- **Merge необратим — всегда подтверждать перед merge.**
- Отвечать по-русски.
